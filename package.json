{
  "scripts": {
    "install": "conan install . --build=missing",
    "build": "cmake -S . --preset conan-debug",
    "build:d": "cmake -S . --preset conan-default",
    "build:r": "cmake -S . --preset conan-release"
  }
}
